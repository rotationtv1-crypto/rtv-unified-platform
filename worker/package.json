{
  "name": "@rotationtv/worker",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "db:migrate": "wrangler d1 migrations apply rotationtv-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply rotationtv-db",
    "db:generate": "wrangler d1 migrations create rotationtv-db"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250320.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
