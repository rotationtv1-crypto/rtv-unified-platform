{
  "name": "@rotationtv/worker",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "db:migrate": "wrangler d1 migrations apply rtv-prod-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply rtv-prod-db",
    "db:generate": "wrangler d1 migrations create rtv-prod-db"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
